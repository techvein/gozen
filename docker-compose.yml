web:
  container_name: web
  image: golang:1.7.3
  ports:
    - "5000:9000"
  volumes:
    - ./:/go/src/gozen
  working_dir: "/go/src/gozen"
  links:
    - db
  tty: true
  command: /bin/bash

db:
  container_name: db
  image: mysql:5.7
  environment:
    MYSQL_ROOT_PASSWORD: rootpass
    MYSQL_DATABASE: gozen
    MYSQL_USER: gozen
    MYSQL_PASSWORD: rootpass
  volumes:
    - ./mysql:/etc/mysql/conf.d
    - /var/lib/mysql


#web:
#  build: .
#  ports:
#    - "5000:9000"
#  volumes:
##    - ./:/myproject/gopath/src/gozen
#    - /var/lib/mysql
#  links:
#    - db
#    # - syslog
#  tty: true
#  command: /bin/bash
#  # command: bash ./build.sh
#
#db:
#  build: ./mysql
#  ports:
#    - "3306:3306"
#  environment:
#    MYSQL_ROOT_PASSWORD: rootpass
#    MYSQL_DATABASE: gozen
#    MYSQL_USER: gozen
#    MYSQL_PASSWORD: rootpass
#      # MYSQL_HOST: ""


# syslog:
#   build: ./syslog
#   ports:
#     - "514:514"
#   tty: true

# db:
#   image: mysql
#   expose:
#     - 3306
#   volumes:
#       - ./my.cnf:/etc/mysql/conf.d/my.cnf
#   container_name: mysql
#   environment:
#     MYSQL_USER: gozen
#     MYSQL_PASSWORD: rootpass
#     MYSQL_ROOT_PASSWORD: rootpass
#     MYSQL_DATABASE: gozen