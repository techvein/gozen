machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t ci .
test:
  override:
    - docker run ci go version && go get -u github.com/golang/dep/... && dep init && go test ./sample
