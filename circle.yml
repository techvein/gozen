machine:
  services:
    - docker
dependencies:
  override:
    - go get -u github.com/golang/dep/... &&  dep ensure -update
    - docker build -t ci .
test:
  override:
    - docker run ci go test ./sample
