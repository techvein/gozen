machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
    - sudo pip install docker-compose
  services:
    - docker

test:
  override:
    - docker-compose up -d
    - docker-compose run web sh '-c' 'go get bitbucket.org/liamstask/goose/cmd/goose && go get -u github.com/kardianos/govendor && goose up && govendor sync && go test  ./controllers -bench -benchmem'
